services:
  frontend:
    image: data-management-frontend
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/src
      - /src/node_modules:/node_modules
    command: ["npm", "run", "dev"]

