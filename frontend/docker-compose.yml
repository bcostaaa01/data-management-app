services:
  frontend:
    image: data-management-frontend
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - VITE_HOST=0.0.0.0 
    volumes:
      - .:/app
    command: ["npm", "run", "dev"]